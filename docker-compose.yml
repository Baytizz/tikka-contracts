services:
  oracle:
    build: ./oracle
    env_file: .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f 'node dist/index.js' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
